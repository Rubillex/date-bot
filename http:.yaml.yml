http:
  routers:
    date-bot:
      rule: "Host(`rubillex.tech`)"
      entrypoints:
        - https
      tls:
        certResolver: letsencrypt
      service: nuxt-basic-frontend

  services:
    nuxt-basic-frontend:
      loadBalancer:
        servers:
          - url: "http://0.0.0.0:3002"
